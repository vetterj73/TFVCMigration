#pragma once

#include <string>
#include <vector>
#include <list>
#include <queue>
#include <map>
#include <fstream>
#include <sstream>

using std::string;
using std::vector;
using std::list;
using std::queue;
using std::map;
using std::pair;
using std::ifstream;
using std::ofstream;
using std::stringstream;